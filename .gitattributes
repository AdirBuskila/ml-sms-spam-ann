# Normalise line endings so Windows/macOS collaborators do not produce noisy diffs
* text=auto eol=lf
*.ipynb text eol=lf
*.pdf binary
*.png binary
# Raw Kaggle files must stay byte-identical to the download (CRLF included)
data/raw/*.csv -text
